
[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="cunw-calibration-reliability"
version="1.0.0"
description="Reproducible regime-aware calibration and inverse-quantification analysis."
requires-python=">=3.11"
dependencies=["numpy==2.3.5","scipy==1.16.3","matplotlib==3.10.8"]
[tool.setuptools.packages.find]
where=["src"]
